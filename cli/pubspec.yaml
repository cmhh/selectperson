name: cli
publish_to: none
description: Example logic for selecting sampling units with unequal probability.
version: 1.0.0

environment:
  sdk: ^3.13.4

dependencies:
  path: ^1.9.0
  select_person:
    path: ../select_person

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
